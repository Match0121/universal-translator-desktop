# 万能翻译站 · UI 重设计说明（高级简约 · premium minimalism）

## 设计理念（一句话）

> 把界面退后，让文本与工作本身成为主角——安静、精确、贵。

上一版是「深夜亮着青灯的仪器」；这一版是「晨光里的一张白纸」：界面只提供秩序和呼吸，不提供氛围。用户看到的应该是译文，而不是界面。

## 色板

### 浅色（主推）

| 色值 | 用途 |
| --- | --- |
| `#F6F5F2` | 页面底色 · 暖灰纸感，替代刺眼纯白，久看不累 |
| `#FFFFFF` | 卡片 / 面板 |
| `#F0EEE9` | 悬停 / 浅填充（导航激活、行 hover、seg 选中） |
| `#26241F` | 主文字 · 暖墨色，比纯黑柔和 |
| `#6E6A61` | 次级文字（meta、说明） |
| `#A6A197` | 弱文字（占位符、行号、版本号） |
| `#E7E4DD` / `#D8D4CB` | 1px 细边框（常规 / 强调） |
| `#4E6C80` | 点缀色 · 雾钢蓝（状态点、链接、聚焦环，克制使用） |
| `#2B2925` | 主按钮底（墨），配白字 —— 高级感的关键：主按钮用墨色而非强调色 |
| `#4F7A66` / `#9A7B3C` / `#A85C4D` | 已翻译 / 提示 / 异常（低饱和莫兰迪系，不抢戏） |

### 深色（亮点演示）

不是简单反色，而是暖石墨基底：`#1B1A18` 底 / `#232220` 卡 / `#E9E6DF` 文 / `#8FA9B8` 雾蓝提亮。主按钮反转（暖白底墨字）。深色下同样没有光晕，只有 1px 细线和轻阴影。

## 字体层级（细字重变化）

| 角色 | 字号 / 字重 |
| --- | --- |
| 页面 / 区块标题 | 17–20px / 600 |
| 文件名、按钮、导航 | 13–14px / 500 |
| 正文、译文 | 14px / 400（正文轻） |
| 原文行 | 14px / 400 · 次级色（与墨色译文拉开，靠色不靠重） |
| meta、提示、说明 | 12.5px / 400 · 次级色 |
| 徽标 / 状态标签 | 11px / 500 · 字距 +0.04em |
| 路径、行号、大小、进度 | 等宽 12–12.5px（"仪器感"的唯一残留，只留给数据） |

层级靠「字重 + 灰度」双轴传达，不靠字号轰炸。

## 签名元素

1. **品牌墨印章**：方块内一个「译」字，右下角嵌一粒雾钢蓝——上一版品牌品红彩蛋的收敛版。
2. **译文区书页式排版**：行号栏（等宽、弱灰、右对齐）+ 一条 1px 竖细线，原文在上（灰）、译文在下（墨），行距从容。翻译浏览本身成为记忆点。
3. **底部细说明条**：仅预览页存在，一条 1px 分隔线 + 44px 高度的安静提示，不打扰主界面。

## 与旧风格对比

### 收敛了

| 旧（深夜青灯 · 仪器感） | 新（高级简约） |
| --- | --- |
| 霓虹青 `#2cc6e0` + 光晕 glow | 雾钢蓝点缀，零光晕 |
| 重阴影（深色下沉感） | 轻阴影，只做层次不做浮起 |
| 毛玻璃顶栏 | 纯色 + 1px 细线 |
| emoji 图标（☀️ ⚙ 📂 🎮） | 细线 SVG 图标（1.5px stroke） |
| 深色默认 | 浅色默认（暖灰纸感） |
| 数据密集、紧凑字号（11–22px） | 从容留白、字重分层 |
| 主按钮用青填充 | 主按钮用墨色，强调色退居点缀 |

### 提升了

- **信息层级**：从「颜色区分」转向「字重 + 灰度」双轴，安静但清晰。
- **状态传达**：已翻译 / 部分 / 未翻译用 6px 色点 + 徽标 + 行数进度，一眼可读。
- **品牌**：从 emoji 化收敛为印章 + 一粒钢蓝，更耐看。
- **翻译浏览体验**：行号 + 原文/译文书页式排版，成为界面唯一的"个性"。
- **双主题一致性**：深浅两套 token 同一套结构，深色不再是附属品。

## 预览页说明（preview.html）

自包含单文件（内嵌 style，零外部资源），静态模拟主界面，可交互：

- 顶栏：品牌印章 + 导航（文档 / 游戏 / 图片）+ 主题切换 + 设置
- 扫描阶段：游戏卡片、资源包提示条、「扫描文本文件」标题行、文件列表（7 个模拟文件）
- 浏览阶段：工具栏（返回 / 视图切换 / 筛选 / 搜索 / 导出）+ 左侧文件树 + 右侧译文单元
- 设置抽屉：引擎 / 目标语言 / 并发 / 上限 + 折叠说明
- 交互：点「提取并翻译」进入浏览；顶栏切换深浅主题；右上角开设置；仅原文 / 对照 / 仅译文可切换；筛选 chips 真实过滤

未改动 `index.html` / `css/` / `js/` 任何实装文件。
